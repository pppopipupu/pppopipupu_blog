import { Metadata } from "next";
import MainFool from "@/components/MainFool";

export const metadata: Metadata = {
  title: "Spell Lab Pro -  pppopipupu",
  description: "全新 M4 秘术芯片。为毁灭而生。",
};

export default function MainFoolPage() {
  return <MainFool />;
}
